import styles from '../../../css/Editor.module.css'

const EditorReviewHistory = () => {
    return ( 
        <div className={`${styles.iconHistory}`} style={{padding: '30px'}}>
            <h1>This is review History</h1> <br />

            <i className={`${styles.iconHistory} fa-solid fa-file-circle-question`} ></i>
            Lorem Inspum Doloe
        </div>
     );
}
 
export default EditorReviewHistory;